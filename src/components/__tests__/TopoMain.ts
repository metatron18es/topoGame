import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import TopoMain from '../TopoMain.vue'

describe('TopoMain', () => {
  it('renders properly', () => {
    const wrapper = mount(TopoMain)
  })
})
